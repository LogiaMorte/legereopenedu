---
import Layout from '../layouts/Layout.astro';
import { useTranslations } from '../i18n/utils';
const lang = 'tr' as const;
const t = useTranslations(lang);
---

<Layout lang={lang} pageTitle="Profilim" noindex>
  <div class="relative min-h-screen pt-24 pb-16 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto">
      <a href="/" class="inline-flex items-center gap-2 text-gold-500 hover:text-gold-300 transition-colors font-heading text-sm mb-8 group">
        <svg class="w-4 h-4 group-hover:-translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5 3 12m0 0 7.5-7.5M3 12h18" />
        </svg>
        {t('login.back')}
      </a>

      <!-- Not Logged In State -->
      <div id="profile-not-logged-in" class="hidden">
        <div class="glass gold-glow rounded-2xl p-8 text-center max-w-md mx-auto">
          <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-gold-500/10 border border-gold-500/20 mb-4">
            <svg class="w-8 h-8 text-gold-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 10.5V6.75a4.5 4.5 0 1 0-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 0 0 2.25-2.25v-6.75a2.25 2.25 0 0 0-2.25-2.25H6.75a2.25 2.25 0 0 0-2.25 2.25v6.75a2.25 2.25 0 0 0 2.25 2.25Z" />
            </svg>
          </div>
          <h1 class="text-xl font-heading font-bold text-text-primary mb-2">{t('profile.notLoggedIn')}</h1>
          <a href="/login" class="btn-gold justify-center text-sm py-3 mt-4 inline-flex">
            {t('profile.goLogin')}
          </a>
        </div>
      </div>

      <!-- Loading State -->
      <div id="profile-loading" class="text-center py-20">
        <div class="inline-flex items-center gap-3 text-text-secondary font-heading text-sm">
          <svg class="w-5 h-5 animate-spin" fill="none" viewBox="0 0 24 24">
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path>
          </svg>
          Yükleniyor...
        </div>
      </div>

      <!-- Profile Content -->
      <div id="profile-content" class="hidden space-y-6">

        <!-- User Info Card -->
        <div class="glass gold-glow rounded-2xl p-6 sm:p-8">
          <div class="flex flex-col sm:flex-row sm:items-start gap-6">
            <div class="shrink-0">
              <div class="w-20 h-20 rounded-full bg-gold-500/10 border-2 border-gold-500/30 flex items-center justify-center">
                <span id="profile-avatar" class="text-3xl font-heading font-bold text-gold-500"></span>
              </div>
            </div>
            <div class="flex-1 min-w-0">
              <div class="flex flex-wrap items-center gap-3 mb-1">
                <h1 id="profile-name" class="text-2xl font-heading font-bold text-text-primary"></h1>
                <div id="profile-verification-badges" class="flex items-center gap-1.5"></div>
              </div>
              <div class="flex flex-wrap items-center gap-x-4 gap-y-1 text-sm text-text-secondary mb-4">
                <span id="profile-university" class="flex items-center gap-1.5">
                  <svg class="w-4 h-4 text-gold-500/60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4.26 10.147a60.438 60.438 0 0 0-.491 6.347A48.62 48.62 0 0 1 12 20.904a48.62 48.62 0 0 1 8.232-4.41 60.46 60.46 0 0 0-.491-6.347m-15.482 0a50.636 50.636 0 0 0-2.658-.813A59.906 59.906 0 0 1 12 3.493a59.903 59.903 0 0 1 10.399 5.84c-.896.248-1.783.52-2.658.814m-15.482 0A50.717 50.717 0 0 1 12 13.489a50.702 50.702 0 0 1 7.74-3.342" />
                  </svg>
                  <span id="profile-uni-text"></span>
                </span>
                <span id="profile-department" class="flex items-center gap-1.5">
                  <svg class="w-4 h-4 text-gold-500/60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M9.568 3H5.25A2.25 2.25 0 0 0 3 5.25v4.318c0 .597.237 1.17.659 1.591l9.581 9.581c.699.699 1.78.872 2.607.33a18.095 18.095 0 0 0 5.223-5.223c.542-.827.369-1.908-.33-2.607L11.16 3.66A2.25 2.25 0 0 0 9.568 3Z" />
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 6h.008v.008H6V6Z" />
                  </svg>
                  <span id="profile-dept-text"></span>
                </span>
                <span id="profile-joindate" class="flex items-center gap-1.5">
                  <svg class="w-4 h-4 text-gold-500/60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 0 1 2.25-2.25h13.5A2.25 2.25 0 0 1 21 7.5v11.25m-18 0A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75m-18 0v-7.5A2.25 2.25 0 0 1 5.25 9h13.5A2.25 2.25 0 0 1 21 11.25v7.5" />
                  </svg>
                  <span>{t('profile.joinDate')}:</span>
                  <span id="profile-join-text"></span>
                </span>
              </div>
              <!-- School Email (if exists) -->
              <span id="profile-school-email" class="hidden flex items-center gap-1.5">
                <svg class="w-4 h-4 text-gold-500/60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 0 1-2.25 2.25h-15a2.25 2.25 0 0 1-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0 0 19.5 4.5h-15a2.25 2.25 0 0 0-2.25 2.25m19.5 0v.243a2.25 2.25 0 0 1-1.07 1.916l-7.5 4.615a2.25 2.25 0 0 1-2.36 0L3.32 8.91a2.25 2.25 0 0 1-1.07-1.916V6.75" />
                </svg>
                <span id="profile-school-email-text"></span>
              </span>
              <!-- Privacy Toggles -->
              <div class="flex flex-wrap gap-4">
                <label class="flex items-center gap-2 text-xs text-text-secondary cursor-pointer group">
                  <input type="checkbox" id="toggle-fullname" class="w-4 h-4 rounded bg-white/5 border-white/20 text-gold-500 focus:ring-gold-500/25 focus:ring-offset-0 accent-[#D4A843]" />
                  <span class="group-hover:text-text-primary transition-colors">{t('profile.showFullName')}</span>
                </label>
                <label class="flex items-center gap-2 text-xs text-text-secondary cursor-pointer group">
                  <input type="checkbox" id="toggle-email" class="w-4 h-4 rounded bg-white/5 border-white/20 text-gold-500 focus:ring-gold-500/25 focus:ring-offset-0 accent-[#D4A843]" />
                  <span class="group-hover:text-text-primary transition-colors">{t('profile.showEmail')}</span>
                </label>
                <label class="flex items-center gap-2 text-xs text-text-secondary cursor-pointer group">
                  <input type="checkbox" id="toggle-public-profile" class="w-4 h-4 rounded bg-white/5 border-white/20 text-gold-500 focus:ring-gold-500/25 focus:ring-offset-0 accent-[#D4A843]" />
                  <span class="group-hover:text-text-primary transition-colors">Profilimi Herkese Aç</span>
                </label>
              </div>
              <!-- Public profile share link (shown when public profile is enabled) -->
              <div id="public-profile-share" class="hidden mt-3 flex items-center gap-2">
                <span class="text-xs text-text-secondary">Paylaş:</span>
                <a id="public-profile-link" href="#" target="_blank" class="text-xs text-gold-500 hover:text-gold-300 transition-colors font-mono truncate max-w-[280px]"></a>
                <button id="copy-public-link" type="button" class="text-xs text-text-secondary hover:text-gold-500 transition-colors" title="Kopyala">
                  <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15.666 3.888A2.25 2.25 0 0 0 13.5 2.25h-3c-1.03 0-1.9.693-2.166 1.638m7.332 0c.055.194.084.4.084.612v0a.75.75 0 0 1-.75.75H9.75a.75.75 0 0 1-.75-.75v0c0-.212.03-.418.084-.612m7.332 0c.646.049 1.288.11 1.927.184 1.1.128 1.907 1.077 1.907 2.185V19.5a2.25 2.25 0 0 1-2.25 2.25H6.75A2.25 2.25 0 0 1 4.5 19.5V6.257c0-1.108.806-2.057 1.907-2.185a48.208 48.208 0 0 1 1.927-.184" />
                  </svg>
                </button>
              </div>
            </div>
          </div>
        </div>

        <!-- Tab Navigation -->
        <div class="flex gap-1 bg-white/[0.02] rounded-xl p-1 border border-white/5">
          <button id="tab-overview" class="profile-tab flex-1 px-4 py-2.5 rounded-lg text-sm font-heading font-medium transition-all" data-tab="overview">
            <svg class="w-4 h-4 inline-block mr-1.5 -mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6A2.25 2.25 0 0 1 6 3.75h2.25A2.25 2.25 0 0 1 10.5 6v2.25a2.25 2.25 0 0 1-2.25 2.25H6a2.25 2.25 0 0 1-2.25-2.25V6ZM3.75 15.75A2.25 2.25 0 0 1 6 13.5h2.25a2.25 2.25 0 0 1 2.25 2.25V18a2.25 2.25 0 0 1-2.25 2.25H6A2.25 2.25 0 0 1 3.75 18v-2.25ZM13.5 6a2.25 2.25 0 0 1 2.25-2.25H18A2.25 2.25 0 0 1 20.25 6v2.25A2.25 2.25 0 0 1 18 10.5h-2.25a2.25 2.25 0 0 1-2.25-2.25V6ZM13.5 15.75a2.25 2.25 0 0 1 2.25-2.25H18a2.25 2.25 0 0 1 2.25 2.25V18A2.25 2.25 0 0 1 18 20.25h-2.25a2.25 2.25 0 0 1-2.25-2.25v-2.25Z" />
            </svg>
            Genel Bakış
          </button>
          <button id="tab-events" class="profile-tab flex-1 px-4 py-2.5 rounded-lg text-sm font-heading font-medium transition-all" data-tab="events">
            <svg class="w-4 h-4 inline-block mr-1.5 -mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 0 1 2.25-2.25h13.5A2.25 2.25 0 0 1 21 7.5v11.25m-18 0A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75m-18 0v-7.5A2.25 2.25 0 0 1 5.25 9h13.5A2.25 2.25 0 0 1 21 11.25v7.5" />
            </svg>
            Etkinliklerim
            <span id="tab-events-count" class="hidden ml-1.5 px-1.5 py-0.5 rounded-full bg-gold-500/20 text-gold-500 text-[10px] font-mono"></span>
          </button>
        </div>

        <!-- Tab: Overview -->
        <div id="panel-overview" class="space-y-6">
          <!-- Stats Row -->
          <div class="grid grid-cols-3 gap-4">
            <div class="glass rounded-xl p-4 text-center">
              <div id="stat-workshops" class="text-2xl font-heading font-bold text-gold-500">0</div>
              <div class="text-xs text-text-secondary font-heading mt-1">{t('profile.workshops')}</div>
            </div>
            <div class="glass rounded-xl p-4 text-center">
              <div id="stat-certificates" class="text-2xl font-heading font-bold text-gold-500">0</div>
              <div class="text-xs text-text-secondary font-heading mt-1">{t('profile.certificates')}</div>
            </div>
            <div class="glass rounded-xl p-4 text-center">
              <div id="stat-badges" class="text-2xl font-heading font-bold text-gold-500">0</div>
              <div class="text-xs text-text-secondary font-heading mt-1">{t('profile.badges')}</div>
            </div>
          </div>

          <!-- Edit Profile Section -->
          <div class="glass rounded-2xl p-6 sm:p-8">
            <button id="edit-toggle" class="w-full flex items-center justify-between text-lg font-heading font-bold text-text-primary">
              <span class="flex items-center gap-2">
                <svg class="w-5 h-5 text-gold-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                  <path stroke-linecap="round" stroke-linejoin="round" d="m16.862 4.487 1.687-1.688a1.875 1.875 0 1 1 2.652 2.652L10.582 16.07a4.5 4.5 0 0 1-1.897 1.13L6 18l.8-2.685a4.5 4.5 0 0 1 1.13-1.897l8.932-8.931Zm0 0L19.5 7.125M18 14v4.75A2.25 2.25 0 0 1 15.75 21H5.25A2.25 2.25 0 0 1 3 18.75V8.25A2.25 2.25 0 0 1 5.25 6H10" />
                </svg>
                Profili Düzenle
              </span>
              <svg id="edit-chevron" class="w-5 h-5 text-text-secondary transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="m19.5 8.25-7.5 7.5-7.5-7.5" />
              </svg>
            </button>
            <div id="edit-form-container" class="hidden mt-6 space-y-4">
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div>
                  <label class="block text-text-secondary text-xs font-heading font-medium mb-1.5">Üniversite</label>
                  <input type="text" id="edit-university" class="w-full px-4 py-3 rounded-lg bg-white/5 border border-white/10 text-text-primary text-sm font-body placeholder:text-text-secondary/40 focus:border-gold-500/50 focus:outline-none focus:ring-1 focus:ring-gold-500/25 transition-colors" />
                </div>
                <div>
                  <label class="block text-text-secondary text-xs font-heading font-medium mb-1.5">Bölüm</label>
                  <input type="text" id="edit-department" class="w-full px-4 py-3 rounded-lg bg-white/5 border border-white/10 text-text-primary text-sm font-body placeholder:text-text-secondary/40 focus:border-gold-500/50 focus:outline-none focus:ring-1 focus:ring-gold-500/25 transition-colors" />
                </div>
              </div>
              <div>
                <label class="block text-text-secondary text-xs font-heading font-medium mb-1.5">Okul E-postası</label>
                <input type="email" id="edit-school-email" class="w-full px-4 py-3 rounded-lg bg-white/5 border border-white/10 text-text-primary text-sm font-body placeholder:text-text-secondary/40 focus:border-gold-500/50 focus:outline-none focus:ring-1 focus:ring-gold-500/25 transition-colors" placeholder="okul@university.edu.tr" />
              </div>
              <div>
                <label class="block text-text-secondary text-xs font-heading font-medium mb-1.5">LinkedIn</label>
                <input type="url" id="edit-linkedin" class="w-full px-4 py-3 rounded-lg bg-white/5 border border-white/10 text-text-primary text-sm font-body placeholder:text-text-secondary/40 focus:border-gold-500/50 focus:outline-none focus:ring-1 focus:ring-gold-500/25 transition-colors" placeholder="linkedin.com/in/..." />
              </div>
              <div>
                <label class="block text-text-secondary text-xs font-heading font-medium mb-1.5">Fikirler & Notlar</label>
                <textarea id="edit-ideas" rows="2" class="w-full px-4 py-3 rounded-lg bg-white/5 border border-white/10 text-text-primary text-sm font-body placeholder:text-text-secondary/40 focus:border-gold-500/50 focus:outline-none focus:ring-1 focus:ring-gold-500/25 transition-colors resize-none"></textarea>
              </div>
              <div>
                <label class="block text-text-secondary text-xs font-heading font-medium mb-2">Katkı Alanları</label>
                <div class="flex flex-wrap gap-2">
                  <label class="flex items-center gap-2 text-sm text-text-secondary cursor-pointer"><input type="checkbox" name="edit-interests" value="projects" class="accent-[#D4A843]" /> Projelerde Yer Almak</label>
                  <label class="flex items-center gap-2 text-sm text-text-secondary cursor-pointer"><input type="checkbox" name="edit-interests" value="workshops" class="accent-[#D4A843]" /> Atölye İçeriği</label>
                  <label class="flex items-center gap-2 text-sm text-text-secondary cursor-pointer"><input type="checkbox" name="edit-interests" value="seminars" class="accent-[#D4A843]" /> Seminer Önermek</label>
                  <label class="flex items-center gap-2 text-sm text-text-secondary cursor-pointer"><input type="checkbox" name="edit-interests" value="content" class="accent-[#D4A843]" /> İçerik Üretmek</label>
                  <label class="flex items-center gap-2 text-sm text-text-secondary cursor-pointer"><input type="checkbox" name="edit-interests" value="mentorship" class="accent-[#D4A843]" /> Mentorluk</label>
                </div>
              </div>
              <div id="edit-error" class="hidden px-4 py-3 rounded-lg bg-red-500/10 border border-red-500/25 text-red-400 text-sm"></div>
              <div id="edit-success" class="hidden px-4 py-3 rounded-lg bg-green-500/10 border border-green-500/25 text-green-400 text-sm">Profil güncellendi!</div>
              <button id="edit-save" class="btn-gold justify-center text-sm py-2.5 px-6">
                <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
                </svg>
                Kaydet
              </button>
            </div>
          </div>

          <!-- Contribution Preferences Section -->
          <div id="contribution-section" class="hidden glass rounded-2xl p-6 sm:p-8">
            <h2 class="text-lg font-heading font-bold text-text-primary mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-gold-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9.813 15.904 9 18.75l-.813-2.846a4.5 4.5 0 0 0-3.09-3.09L2.25 12l2.846-.813a4.5 4.5 0 0 0 3.09-3.09L9 5.25l.813 2.846a4.5 4.5 0 0 0 3.09 3.09L15.75 12l-2.846.813a4.5 4.5 0 0 0-3.09 3.09ZM18.259 8.715 18 9.75l-.259-1.035a3.375 3.375 0 0 0-2.455-2.456L14.25 6l1.036-.259a3.375 3.375 0 0 0 2.455-2.456L18 2.25l.259 1.035a3.375 3.375 0 0 0 2.455 2.456L21.75 6l-1.036.259a3.375 3.375 0 0 0-2.455 2.456ZM16.894 20.567 16.5 21.75l-.394-1.183a2.25 2.25 0 0 0-1.423-1.423L13.5 18.75l1.183-.394a2.25 2.25 0 0 0 1.423-1.423l.394-1.183.394 1.183a2.25 2.25 0 0 0 1.423 1.423l1.183.394-1.183.394a2.25 2.25 0 0 0-1.423 1.423Z" />
              </svg>
              Katkı Tercihleri
            </h2>
            <div id="contribution-interests" class="flex flex-wrap gap-2 mb-4"></div>
            <div id="contribution-ideas" class="hidden">
              <p class="text-xs text-text-secondary font-heading font-medium mb-1.5">Fikirler & Notlar:</p>
              <p id="contribution-ideas-text" class="text-sm text-text-secondary/80 leading-relaxed"></p>
            </div>
            <div id="contribution-linkedin" class="hidden mt-3">
              <a id="contribution-linkedin-link" href="#" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-1.5 text-xs text-gold-500 hover:text-gold-300 transition-colors font-heading">
                <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
                LinkedIn Profili
              </a>
            </div>
            <div class="mt-4 px-4 py-3 rounded-lg bg-gold-500/5 border border-gold-500/15 text-xs text-text-secondary leading-relaxed">
              <span class="text-gold-500 font-heading font-medium">📬</span>
              Katılmak istediğiniz programlar ve fikirleriniz hakkında bize <a href="mailto:info@legereopenedu.com" class="text-gold-500 hover:text-gold-300 transition-colors">info@legereopenedu.com</a> adresinden yazabilirsiniz.
            </div>
          </div>

          <!-- Certificates Section -->
          <div class="glass rounded-2xl p-6 sm:p-8">
            <h2 class="text-lg font-heading font-bold text-text-primary mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-gold-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 14.25v-2.625a3.375 3.375 0 0 0-3.375-3.375h-1.5A1.125 1.125 0 0 1 13.5 7.125v-1.5a3.375 3.375 0 0 0-3.375-3.375H8.25m0 12.75h7.5m-7.5 3H12M10.5 2.25H5.625c-.621 0-1.125.504-1.125 1.125v17.25c0 .621.504 1.125 1.125 1.125h12.75c.621 0 1.125-.504 1.125-1.125V11.25a9 9 0 0 0-9-9Z" />
              </svg>
              {t('profile.certificates')}
              <span id="cert-count" class="text-sm font-normal text-text-secondary"></span>
            </h2>
            <div id="certificates-list" class="space-y-3">
              <p class="text-sm text-text-secondary">{t('profile.noCerts')}</p>
            </div>
          </div>

          <!-- Badges Section -->
          <div class="glass rounded-2xl p-6 sm:p-8">
            <h2 class="text-lg font-heading font-bold text-text-primary mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-gold-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 18.75h-9m9 0a3 3 0 0 1 3 3h-15a3 3 0 0 1 3-3m9 0v-3.375c0-.621-.503-1.125-1.125-1.125h-.871M7.5 18.75v-3.375c0-.621.504-1.125 1.125-1.125h.872m5.007 0H9.497m5.007 0a7.454 7.454 0 0 1-.982-3.172M9.497 14.25a7.454 7.454 0 0 0 .981-3.172M5.25 4.236c-.982.143-1.954.317-2.916.52A6.003 6.003 0 0 0 7.73 9.728M5.25 4.236V4.5c0 2.108.966 3.99 2.48 5.228M5.25 4.236V2.721C7.456 2.41 9.71 2.25 12 2.25c2.291 0 4.545.16 6.75.47v1.516M18.75 4.236c.982.143 1.954.317 2.916.52A6.003 6.003 0 0 1 16.27 9.728M18.75 4.236V4.5c0 2.108-.966 3.99-2.48 5.228m0 0a6.023 6.023 0 0 1-2.77.896m5.25-6.388V2.721" />
              </svg>
              {t('profile.badges')}
              <span id="badge-count" class="text-sm font-normal text-text-secondary"></span>
            </h2>
            <div id="badges-list" class="flex flex-wrap gap-3">
              <p class="text-sm text-text-secondary">{t('profile.noBadges')}</p>
            </div>
          </div>
        </div>

        <!-- Tab: Events -->
        <div id="panel-events" class="hidden space-y-6">
          <!-- Upcoming / Active Events -->
          <div class="glass rounded-2xl p-6 sm:p-8">
            <h2 class="text-lg font-heading font-bold text-text-primary mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-status-upcoming" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
              </svg>
              Yaklaşan & Aktif Etkinlikler
            </h2>
            <div id="events-upcoming" class="space-y-3">
              <p class="text-sm text-text-secondary">Yaklaşan etkinliğiniz yok.</p>
            </div>
          </div>

          <!-- Pending Applications -->
          <div id="events-pending-section" class="hidden glass rounded-2xl p-6 sm:p-8">
            <h2 class="text-lg font-heading font-bold text-text-primary mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-yellow-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
              </svg>
              Bekleyen Başvurular
            </h2>
            <div id="events-pending" class="space-y-3"></div>
          </div>

          <!-- Past Events -->
          <div class="glass rounded-2xl p-6 sm:p-8">
            <h2 class="text-lg font-heading font-bold text-text-primary mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-status-open" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
              </svg>
              Geçmiş Etkinlikler
            </h2>
            <div id="events-past" class="space-y-3">
              <p class="text-sm text-text-secondary">Tamamlanmış etkinliğiniz yok.</p>
            </div>
          </div>

          <!-- Browse events CTA -->
          <div class="text-center">
            <a href="/#workshops" class="btn-outline text-sm py-2.5 px-6 inline-flex items-center gap-2">
              <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 6.042A8.967 8.967 0 0 0 6 3.75c-1.052 0-2.062.18-3 .512v14.25A8.987 8.987 0 0 1 6 18c2.305 0 4.408.867 6 2.292m0-14.25a8.966 8.966 0 0 1 6-2.292c1.052 0 2.062.18 3 .512v14.25A8.987 8.987 0 0 0 18 18a8.967 8.967 0 0 0-6 2.292m0-14.25v14.25" />
              </svg>
              Etkinliklere Göz At
            </a>
          </div>
        </div>

        <!-- Logout -->
        <div class="text-center pt-4">
          <button id="logout-btn" class="btn-outline text-sm py-2.5 px-6">
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 9V5.25A2.25 2.25 0 0 0 13.5 3h-6a2.25 2.25 0 0 0-2.25 2.25v13.5A2.25 2.25 0 0 0 7.5 21h6a2.25 2.25 0 0 0 2.25-2.25V15m3 0 3-3m0 0-3-3m3 3H9" />
            </svg>
            {t('profile.logout')}
          </button>
        </div>
      </div>
    </div>
  </div>
</Layout>

<script data-cfasync="false" is:inline>
document.addEventListener('astro:page-load', function() {
  var BADGE_META = {
    'first-workshop':   { icon: '🎓', name: 'İlk Adım', desc: 'İlk atölyeyi tamamladı' },
    'three-workshops':  { icon: '⭐', name: 'Araştırmacı', desc: 'Üç atölye tamamladı' },
    'five-workshops':   { icon: '🏆', name: 'Uzman Araştırmacı', desc: 'Beş atölye tamamladı' },
    'multi-discipline': { icon: '🔗', name: 'Disiplinlerarası', desc: '3+ farklı disiplinden atölyelere katıldı' },
    'pioneer':          { icon: '🚀', name: 'Öncü', desc: 'Kurucu dönem katılımcısı' },
    'mentor':           { icon: '🧭', name: 'Mentor', desc: 'Mentorluk katkısında bulundu' },
    'contributor':      { icon: '✍️', name: 'Katkıcı', desc: 'Yayın sürecine aktif katkı' },
    'scholar':          { icon: '📖', name: 'Akademisyen', desc: 'Akademik yayın üretti' },
  };
  var CERT_TYPES = {
    'participation': 'Katılım Sertifikası',
    'achievement':   'Başarı Sertifikası',
    'contribution':  'Katkı Sertifikası',
  };
  var STATUS_MAP = {
    'completed': { label: 'Tamamlandı', cls: 'badge-open' },
    'accepted':  { label: 'Kabul Edildi', cls: 'badge-upcoming' },
    'pending':   { label: 'Beklemede', cls: 'badge-discipline' },
    'rejected':  { label: 'Reddedildi', cls: 'badge-closed' },
  };
  var INTEREST_LABELS = {
    'projects': 'Projelerde Yer Almak',
    'workshops': 'Atölye İçeriği Üretmek',
    'seminars': 'Seminer / Kolokyum Önermek',
    'content': 'İçerik Üretmek',
    'mentorship': 'Mentorluk Yapmak',
    'other': 'Diğer',
  };

  // HTML escape utility to prevent XSS
  function esc(str) {
    if (!str) return '';
    return String(str).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');
  }

  var loadingEl = document.getElementById('profile-loading');
  var contentEl = document.getElementById('profile-content');
  var notLoggedEl = document.getElementById('profile-not-logged-in');

  // Tab switching
  var currentTab = 'overview';
  document.querySelectorAll('.profile-tab').forEach(function(tab) {
    tab.addEventListener('click', function() {
      switchTab(tab.dataset.tab);
    });
  });

  function switchTab(tabName) {
    currentTab = tabName;
    document.querySelectorAll('.profile-tab').forEach(function(t) {
      if (t.dataset.tab === tabName) {
        t.classList.add('bg-gold-500/10', 'text-gold-500', 'border', 'border-gold-500/20');
        t.classList.remove('text-text-secondary');
      } else {
        t.classList.remove('bg-gold-500/10', 'text-gold-500', 'border', 'border-gold-500/20');
        t.classList.add('text-text-secondary');
      }
    });
    document.getElementById('panel-overview').classList.toggle('hidden', tabName !== 'overview');
    document.getElementById('panel-events').classList.toggle('hidden', tabName !== 'events');
  }

  // Init default tab
  switchTab('overview');

  // Loading timeout — 10s max
  var loadTimeout = setTimeout(function() {
    if (loadingEl && !loadingEl.classList.contains('hidden')) {
      loadingEl.classList.add('hidden');
      notLoggedEl.classList.remove('hidden');
    }
  }, 10000);

  async function loadProfile() {
    try {
      var res = await fetch('/api/auth/me', { credentials: 'same-origin' });
      clearTimeout(loadTimeout);
      if (!res.ok) {
        loadingEl.classList.add('hidden');
        notLoggedEl.classList.remove('hidden');
        return;
      }
      var data = await res.json();
      renderProfile(data);
      loadingEl.classList.add('hidden');
      contentEl.classList.remove('hidden');
    } catch (err) {
      clearTimeout(loadTimeout);
      loadingEl.classList.add('hidden');
      notLoggedEl.classList.remove('hidden');
    }
  }

  function renderProfile(data) {
    var user = data.user;
    var registrations = data.registrations;
    var certificates = data.certificates;
    var badges = data.badges;

    // Avatar — use picture if available (safe: createElement instead of innerHTML)
    if (user.picture) {
      var avatarParent = document.getElementById('profile-avatar').parentElement;
      var img = document.createElement('img');
      img.src = user.picture;
      img.alt = '';
      img.className = 'w-20 h-20 rounded-full object-cover';
      avatarParent.textContent = '';
      avatarParent.appendChild(img);
    } else {
      var initials = user.name ? user.name.split(' ').map(function(w) { return w[0]; }).join('').toUpperCase().slice(0, 2) : '?';
      document.getElementById('profile-avatar').textContent = initials;
    }
    document.getElementById('profile-name').textContent = user.name || 'Üye';
    document.getElementById('profile-uni-text').textContent = user.university || '-';
    document.getElementById('profile-dept-text').textContent = user.department || user.linkedinHeadline || '-';
    document.getElementById('profile-join-text').textContent = user.joinDate ? new Date(user.joinDate).toLocaleDateString('tr-TR', { year: 'numeric', month: 'long' }) : '-';

    // Verification badges (safe HTML — only static content, no user data in attributes)
    var badgesContainer = document.getElementById('profile-verification-badges');
    var badgeHtml = '';

    if (user.linkedinSub) {
      badgeHtml += '<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-heading font-semibold bg-[#0A66C2]/15 text-[#0A66C2] border border-[#0A66C2]/25" title="LinkedIn ile giriş yapıldı"><svg class="w-3 h-3" fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>LinkedIn</span>';
    }

    if (user.googleSub) {
      badgeHtml += '<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-heading font-semibold bg-white/10 text-text-secondary border border-white/15" title="Google ile giriş yapıldı"><svg class="w-3 h-3" viewBox="0 0 24 24"><path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92a5.06 5.06 0 0 1-2.2 3.32v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.1z"/><path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/><path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/><path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/></svg>Google</span>';
    }

    // LinkedIn Verified badges
    var verifications = user.linkedinVerifications || [];
    var VERIFY_LABELS = {
      'IDENTITY': 'Kimlik Doğrulandı',
      'EMPLOYMENT': 'Kurum Doğrulandı',
      'EDUCATION': 'Eğitim Doğrulandı',
    };
    verifications.forEach(function(v) {
      var label = VERIFY_LABELS[v] || esc(v);
      badgeHtml += '<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-heading font-semibold bg-emerald-500/15 text-emerald-400 border border-emerald-500/25" title="LinkedIn tarafından doğrulanmış: ' + esc(label) + '"><svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" /></svg>' + esc(label) + '</span>';
    });

    badgesContainer.innerHTML = badgeHtml;

    // School email
    if (user.schoolEmail) {
      document.getElementById('profile-school-email').classList.remove('hidden');
      document.getElementById('profile-school-email-text').textContent = user.schoolEmail;
    }

    // Privacy toggles with error recovery
    var toggleFullName = document.getElementById('toggle-fullname');
    var toggleEmail = document.getElementById('toggle-email');
    var togglePublicProfile = document.getElementById('toggle-public-profile');
    toggleFullName.checked = user.showFullName ?? true;
    toggleEmail.checked = user.showEmail ?? false;
    togglePublicProfile.checked = user.showPublicProfile ?? false;
    toggleFullName.addEventListener('change', function() { updatePrivacy({ showFullName: toggleFullName.checked }, toggleFullName); });
    toggleEmail.addEventListener('change', function() { updatePrivacy({ showEmail: toggleEmail.checked }, toggleEmail); });
    togglePublicProfile.addEventListener('change', function() {
      updatePrivacy({ showPublicProfile: togglePublicProfile.checked }, togglePublicProfile, function() {
        // On success, reload to get updated publicProfileId
        setTimeout(function() { window.location.reload(); }, 800);
      });
    });

    // Public profile share link
    if (user.showPublicProfile && user.publicProfileId) {
      var shareContainer = document.getElementById('public-profile-share');
      var shareLink = document.getElementById('public-profile-link');
      var publicUrl = window.location.origin + '/m?id=' + user.publicProfileId;
      shareContainer.classList.remove('hidden');
      shareLink.href = publicUrl;
      shareLink.textContent = publicUrl;
      document.getElementById('copy-public-link').addEventListener('click', function() {
        navigator.clipboard.writeText(publicUrl).then(function() {
          var btn = document.getElementById('copy-public-link');
          btn.innerHTML = '<svg class="w-3.5 h-3.5 text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" /></svg>';
          setTimeout(function() {
            btn.innerHTML = '<svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M15.666 3.888A2.25 2.25 0 0 0 13.5 2.25h-3c-1.03 0-1.9.693-2.166 1.638m7.332 0c.055.194.084.4.084.612v0a.75.75 0 0 1-.75.75H9.75a.75.75 0 0 1-.75-.75v0c0-.212.03-.418.084-.612m7.332 0c.646.049 1.288.11 1.927.184 1.1.128 1.907 1.077 1.907 2.185V19.5a2.25 2.25 0 0 1-2.25 2.25H6.75A2.25 2.25 0 0 1 4.5 19.5V6.257c0-1.108.806-2.057 1.907-2.185a48.208 48.208 0 0 1 1.927-.184" /></svg>';
          }, 2000);
        });
      });
    }

    // Populate edit form
    document.getElementById('edit-university').value = user.university || '';
    document.getElementById('edit-department').value = user.department || '';
    document.getElementById('edit-school-email').value = user.schoolEmail || '';
    document.getElementById('edit-linkedin').value = user.linkedin || '';
    document.getElementById('edit-ideas').value = user.ideas || '';
    if (user.interests) {
      user.interests.forEach(function(i) {
        var cb = document.querySelector('input[name="edit-interests"][value="' + i + '"]');
        if (cb) cb.checked = true;
      });
    }

    // Contribution preferences
    if ((user.interests && user.interests.length > 0) || user.ideas || user.linkedin) {
      var contSection = document.getElementById('contribution-section');
      contSection.classList.remove('hidden');
      if (user.interests && user.interests.length > 0) {
        var contInterests = document.getElementById('contribution-interests');
        contInterests.innerHTML = user.interests.map(function(i) {
          var label = INTEREST_LABELS[i] || esc(i);
          return '<span class="badge badge-discipline">' + esc(label) + '</span>';
        }).join('');
      }
      if (user.ideas) {
        document.getElementById('contribution-ideas').classList.remove('hidden');
        document.getElementById('contribution-ideas-text').textContent = user.ideas;
      }
      if (user.linkedin) {
        document.getElementById('contribution-linkedin').classList.remove('hidden');
        document.getElementById('contribution-linkedin-link').href = user.linkedin;
      }
    }

    // Stats
    document.getElementById('stat-workshops').textContent = registrations.length;
    document.getElementById('stat-certificates').textContent = certificates.length;
    document.getElementById('stat-badges').textContent = badges.length;

    // Events tab count
    if (registrations.length > 0) {
      var countEl = document.getElementById('tab-events-count');
      countEl.textContent = registrations.length;
      countEl.classList.remove('hidden');
    }

    // Certificates (XSS-safe)
    if (certificates.length > 0) {
      document.getElementById('cert-count').textContent = '(' + certificates.length + ')';
      var certList = document.getElementById('certificates-list');
      certList.innerHTML = certificates.map(function(cert) {
        var typeLabel = CERT_TYPES[cert.type] || esc(cert.type);
        var typeColor = cert.type === 'achievement' ? 'text-gold-300' : cert.type === 'contribution' ? 'text-status-upcoming' : 'text-status-open';
        return '<div class="glass-subtle p-4 flex flex-col sm:flex-row sm:items-center justify-between gap-3"><div class="min-w-0"><div class="flex items-center gap-2 mb-1"><span class="text-sm font-heading font-semibold ' + typeColor + '">' + esc(typeLabel) + '</span><span class="text-xs font-mono text-text-secondary">' + esc(cert.id) + '</span></div><p class="text-xs text-text-secondary truncate">' + esc(cert.workshopTitle || cert.workshopId || '-') + '</p>' + (cert.issueDate ? '<p class="text-xs text-text-secondary/60 mt-0.5">' + new Date(cert.issueDate).toLocaleDateString('tr-TR') + '</p>' : '') + '</div><div class="flex items-center gap-2 shrink-0"><a href="/verify?cert=' + encodeURIComponent(cert.id) + '" class="text-xs text-gold-500 hover:text-gold-300 font-heading transition-colors">Doğrula</a></div></div>';
      }).join('');
    }

    // Badges (XSS-safe)
    if (badges.length > 0) {
      document.getElementById('badge-count').textContent = '(' + badges.length + ')';
      var badgesList = document.getElementById('badges-list');
      badgesList.innerHTML = badges.map(function(b) {
        var meta = BADGE_META[b.badgeId] || { icon: '🎖️', name: b.badgeId, desc: '' };
        return '<div class="glass-subtle px-4 py-3 rounded-xl flex items-center gap-3 group hover:border-gold-500/30 transition-colors" title="' + esc(meta.desc) + '"><span class="text-2xl">' + meta.icon + '</span><div><div class="text-sm font-heading font-semibold text-text-primary">' + esc(meta.name) + '</div><div class="text-xs text-text-secondary">' + esc(meta.desc) + '</div></div></div>';
      }).join('');
    }

    // Events tab: Categorize registrations
    var upcoming = registrations.filter(function(r) { return r.status === 'accepted'; });
    var pending = registrations.filter(function(r) { return r.status === 'pending'; });
    var past = registrations.filter(function(r) { return r.status === 'completed'; });

    function renderEventCard(r) {
      var status = STATUS_MAP[r.status] || STATUS_MAP['pending'];
      return '<div class="glass-subtle p-4 flex flex-col sm:flex-row sm:items-center justify-between gap-2"><div class="min-w-0"><p class="text-sm font-heading font-medium text-text-primary truncate">' + esc(r.workshop || r.id) + '</p>' + (r.timestamp ? '<p class="text-xs text-text-secondary/60 mt-0.5">' + new Date(r.timestamp).toLocaleDateString('tr-TR', { day: 'numeric', month: 'long', year: 'numeric' }) + '</p>' : '') + '</div><span class="badge ' + status.cls + ' shrink-0">' + esc(status.label) + '</span></div>';
    }

    if (upcoming.length > 0) {
      document.getElementById('events-upcoming').innerHTML = upcoming.map(renderEventCard).join('');
    }

    if (pending.length > 0) {
      document.getElementById('events-pending-section').classList.remove('hidden');
      document.getElementById('events-pending').innerHTML = pending.map(renderEventCard).join('');
    }

    if (past.length > 0) {
      document.getElementById('events-past').innerHTML = past.map(renderEventCard).join('');
    }
  }

  // Privacy toggle with rollback on failure
  async function updatePrivacy(settings, toggleEl, onSuccess) {
    var prevState = !toggleEl.checked;
    try {
      var res = await fetch('/api/auth/me', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'same-origin',
        body: JSON.stringify(settings),
      });
      if (!res.ok) {
        toggleEl.checked = prevState;
      } else if (onSuccess) {
        onSuccess();
      }
    } catch (err) {
      toggleEl.checked = prevState;
    }
  }

  // Edit profile toggle
  var editToggle = document.getElementById('edit-toggle');
  var editContainer = document.getElementById('edit-form-container');
  var editChevron = document.getElementById('edit-chevron');
  editToggle.addEventListener('click', function() {
    editContainer.classList.toggle('hidden');
    editChevron.style.transform = editContainer.classList.contains('hidden') ? '' : 'rotate(180deg)';
  });

  // Edit profile save — with loading state
  var saveBtn = document.getElementById('edit-save');
  saveBtn.addEventListener('click', async function() {
    var editError = document.getElementById('edit-error');
    var editSuccess = document.getElementById('edit-success');
    editError.classList.add('hidden');
    editSuccess.classList.add('hidden');

    // Loading state
    saveBtn.disabled = true;
    var origText = saveBtn.innerHTML;
    saveBtn.innerHTML = '<svg class="w-4 h-4 animate-spin" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path></svg> Kaydediliyor...';

    var interests = [];
    document.querySelectorAll('input[name="edit-interests"]:checked').forEach(function(cb) { interests.push(cb.value); });

    try {
      var res = await fetch('/api/auth/me', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'same-origin',
        body: JSON.stringify({
          university: document.getElementById('edit-university').value.trim(),
          department: document.getElementById('edit-department').value.trim(),
          schoolEmail: document.getElementById('edit-school-email').value.trim(),
          linkedin: document.getElementById('edit-linkedin').value.trim(),
          ideas: document.getElementById('edit-ideas').value.trim(),
          interests: interests,
        }),
      });
      if (res.ok) {
        editSuccess.classList.remove('hidden');
        setTimeout(function() { window.location.reload(); }, 1200);
      } else {
        var result = await res.json().catch(function() { return {}; });
        editError.textContent = result.error === 'School email already in use'
          ? 'Bu okul e-postası başka bir hesap tarafından kullanılıyor.'
          : result.error === 'Invalid school email format'
          ? 'Geçerli bir okul e-posta adresi girin.'
          : 'Bir hata oluştu. Lütfen tekrar deneyin.';
        editError.classList.remove('hidden');
      }
    } catch (err) {
      editError.textContent = 'Bağlantı hatası.';
      editError.classList.remove('hidden');
    } finally {
      saveBtn.disabled = false;
      saveBtn.innerHTML = origText;
    }
  });

  // Logout
  document.getElementById('logout-btn')?.addEventListener('click', async function() {
    try {
      await fetch('/api/auth/me', { method: 'DELETE', credentials: 'same-origin' });
    } catch (err) {}
    document.cookie = 'legere_logged_in=; Path=/; Max-Age=0; SameSite=Lax';
    window.location.href = '/';
  });

  if (loadingEl) loadProfile();
});
</script>
