---
import type { Lang } from '../i18n/languages';
import { useTranslations } from '../i18n/utils';

interface Props {
  lang: Lang;
  id: string;
  title: string;
  dateStart: string;
  dateEnd: string;
  disciplines: string[];
  status: 'open' | 'upcoming' | 'completed';
  currentParticipants: number;
  maxParticipants: number;
  formEntryValue: string;
  description: string;
}

const {
  lang,
  id,
  title,
  dateStart,
  dateEnd,
  disciplines,
  status,
  currentParticipants,
  maxParticipants,
  formEntryValue,
  description,
} = Astro.props;

const t = useTranslations(lang);

const statusConfig = {
  open: { class: 'badge-open', label: t('workshops.status.open') },
  upcoming: { class: 'badge-upcoming', label: t('workshops.status.upcoming') },
  completed: { class: 'badge-closed', label: t('workshops.status.completed') },
};

const disciplineLabels: Record<string, Record<string, string>> = {
  criminology: { tr: 'Kriminoloji', en: 'Criminology' },
  sociology: { tr: 'Sosyoloji', en: 'Sociology' },
  philosophy: { tr: 'Felsefe', en: 'Philosophy' },
  'data-science': { tr: 'Veri Bilimi', en: 'Data Science' },
  law: { tr: 'Hukuk', en: 'Law' },
  psychology: { tr: 'Psikoloji', en: 'Psychology' },
  anthropology: { tr: 'Antropoloji', en: 'Anthropology' },
  'political-science': { tr: 'Siyaset Bilimi', en: 'Political Science' },
  communication: { tr: 'İletişim Bilimleri', en: 'Communication Sciences' },
  economics: { tr: 'Ekonomi', en: 'Economics' },
  ai: { tr: 'Yapay Zeka', en: 'Artificial Intelligence' },
  'international-relations': { tr: 'Uluslararası İlişkiler', en: 'International Relations' },
};

const progressPercent = Math.round((currentParticipants / maxParticipants) * 100);

const formatDate = (dateStr: string) => {
  const date = new Date(dateStr);
  return date.toLocaleDateString(lang === 'tr' ? 'tr-TR' : 'en-US', {
    day: 'numeric',
    month: 'short',
    year: 'numeric',
  });
};

// Escape single quotes for JS
const escapedTitle = title.replace(/'/g, "\\'");
const escapedId = id.replace(/'/g, "\\'");
---

<div class="glass p-6 flex flex-col gap-4 min-w-[300px] md:min-w-[350px] hover:gold-glow transition-all duration-300">
  <!-- Header -->
  <div class="flex items-start justify-between gap-3">
    <h3 class="text-lg font-heading font-bold text-text-primary">
      {title}
    </h3>
    <span class={`badge shrink-0 ${statusConfig[status].class}`}>
      {statusConfig[status].label}
    </span>
  </div>

  <!-- Date -->
  <div class="flex items-center gap-2 text-text-secondary text-sm">
    <svg class="w-4 h-4 text-gold-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5A2.25 2.25 0 0121 11.25v7.5" />
    </svg>
    <span>{formatDate(dateStart)} — {formatDate(dateEnd)}</span>
  </div>

  <!-- Description -->
  <p class="text-text-secondary text-sm leading-relaxed">
    {description}
  </p>

  <!-- Discipline tags -->
  <div class="flex flex-wrap gap-2">
    {disciplines.map((d) => (
      <span class="badge badge-discipline text-xs">
        {disciplineLabels[d]?.[lang] ?? d}
      </span>
    ))}
  </div>

  <!-- Participant bar -->
  <div>
    <div class="flex justify-between text-xs text-text-secondary mb-1.5">
      <span>{t('workshops.participants')}</span>
      <span class="font-mono">{currentParticipants}/{maxParticipants}</span>
    </div>
    <div class="w-full h-1.5 bg-white/5 rounded-full overflow-hidden">
      <div
        class="h-full rounded-full bg-gradient-to-r from-gold-600 to-gold-400 transition-all duration-500"
        style={`width: ${progressPercent}%`}
      ></div>
    </div>
  </div>

  <!-- CTA -->
  {status === 'open' && (
    <button
      type="button"
      onclick={`openRegistration('${escapedTitle}', '${escapedId}')`}
      class="btn-gold text-center justify-center text-sm mt-auto cursor-pointer"
      aria-label={`${t('workshops.register')} - ${title}`}
    >
      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M16.862 4.487l1.687-1.688a1.875 1.875 0 112.652 2.652L10.582 16.07a4.5 4.5 0 01-1.897 1.13L6 18l.8-2.685a4.5 4.5 0 011.13-1.897l8.932-8.931zm0 0L19.5 7.125M18 14v4.75A2.25 2.25 0 0115.75 21H5.25A2.25 2.25 0 013 18.75V8.25A2.25 2.25 0 015.25 6H10" />
      </svg>
      {t('workshops.register')}
    </button>
  )}

  {status === 'upcoming' && (
    <div class="btn-outline text-center justify-center text-sm mt-auto cursor-default opacity-70">
      {t('workshops.status.upcoming')}
    </div>
  )}
</div>
